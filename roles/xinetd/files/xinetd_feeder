service dci-feeder
{
    port            = 80
    type            = UNLISTED
    socket_type     = stream
    protocol        = tcp
    wait            = no
    user            = root
    server          = /usr/bin/dci-feeder.sh
}
