{
  "checks": {
    "api": {
      "command": "check-http-json.rb -s -k -u {% if api_ssl is defined %}https{% else %}http{% endif %}://{{api_servername}}/api/v1",
      "interval": 60,
      "occurrences": 5,
      "handlers": ["file"],
      "subscribers": [
        "api"
      ]
    }
  }
}
