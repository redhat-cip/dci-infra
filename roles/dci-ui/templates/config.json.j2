{
  "apiURL": "{% if ssl_enabled|bool %}https{% else %}http{% endif %}://{{ apihost }}",
  "sso": {
    "url": "{{ sso_url }}",
    "realm": "{{ sso_realm }}",
    "clientId": "{{ sso_client_id }}"
  }
}