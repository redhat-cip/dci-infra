[tox]
envlist = dci-feeder
skipsdist = true

[testenv]
basepython = python3
envdir = {toxworkdir}/molecule
deps =
    ansible<2.10
    molecule
    molecule-podman
    pytest
    pytest-testinfra
    yamllint
commands =
    molecule -vv test
changedir =
    {toxinidir}/roles/{envname}
